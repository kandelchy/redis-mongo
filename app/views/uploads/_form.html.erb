<%= form_with(model: upload, local: true) do |form| %>
  <% if upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(upload.errors.count, "error") %> prohibited this upload from being saved:</h2>

      <ul>
      <% upload.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :messagecode %>
    <%= form.text_field :messagecode, id: :upload_messagecode %>
  </div>

  <div class="field">
    <%= form.label :messagetext %>
    <%= form.text_field :messagetext, id: :upload_messagetext %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
